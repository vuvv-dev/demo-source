# ── ACME-only config (temporary, restored after certbot) ───────────────────
server {
    listen 80;
    listen [::]:80;
    server_name __DOMAIN__;

    location /.well-known/acme-challenge/ {
        root __WEBROOT_PATH__;
    }

    location / {
        return 503;
    }
}
